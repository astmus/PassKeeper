<UserControl x:Class="PassKeeper.AddAccount"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="480" d:DesignWidth="480">
    
    <Grid x:Name="LayoutRoot" Background="{StaticResource PhoneChromeBrush}">
        <StackPanel>
            <TextBlock Margin="12,0,0,-10"  Text="{Binding Path=LocalizedResources.Name, Source={StaticResource LocalizedStrings}}" />
            <TextBox x:Name="Name"/>
            <TextBlock Margin="12,0,0,-10"  Text="{Binding Path=LocalizedResources.Login, Source={StaticResource LocalizedStrings}}" />
            <TextBox x:Name="Login"/>
            <TextBlock Margin="12,0,0,-10"  Text="{Binding Path=LocalizedResources.Password, Source={StaticResource LocalizedStrings}}" />
            <TextBox x:Name="Password"/>
        </StackPanel>
    </Grid>
</UserControl>